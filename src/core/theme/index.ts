export {
  lightColors,
  darkColors,
  spacing,
  borderRadius,
  typography,
  shadow,
  theme,
} from '../../shared/theme';

export type { AppColors } from '../../shared/theme';

export { useTheme } from '../../shared/theme/useTheme';
